import user from './user';
import common from './common';
import project from './project';
import auth from './auth';
import feedback from './feedback';

const api = {user, common, project, auth, feedback};

export default api;
